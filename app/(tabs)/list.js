// File path: /linesight/app/(tabs)/list.js
// This is the page where the user can see all tasks in one checklist.
// From here the user can:
//      > check/un-check tasks
//      > delete a task
//      > add a task
//      > go back to the checklists page

import { Text, View } from "react-native"

function List(){
    return(
        <Text>
            test
        </Text>
    )
}

export default List;